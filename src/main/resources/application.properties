# ── Servidor ──
server.port=8081

# ── PostgreSQL ──
spring.datasource.url=jdbc:postgresql://localhost:5436/prueba1
spring.datasource.username=postgres
spring.datasource.password=Popeye08
spring.datasource.driver-class-name=org.postgresql.Driver

# ── JPA ──
spring.jpa.open-in-view=false
spring.jpa.hibernate.ddl-auto=none

# ── Flyway (gestionado manualmente en FlywayConfig.java) ──
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration
spring.flyway.schemas=public
spring.flyway.table=flyway_schema_history

# ── Deshabilitar Config Server ──
spring.cloud.config.enabled=false

# ── GestoPago Auth (Feign) ──
gestopago.auth.url=https://gestopago.portalventas.net
gestopago.auth.id-distribuidor=83
gestopago.auth.codigo-dispositivo=GPS83-TPV-17
gestopago.auth.password=12345678
gestopago.auth.refresh-rate-ms=3600000

# ?? Redis ??
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.timeout=1500ms
spring.data.redis.connect-timeout=1500ms

# TTL del cache de productos en segundos (1 hora)
app.cache.productos.ttl-seconds=3600